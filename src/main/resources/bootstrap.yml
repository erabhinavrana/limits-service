spring:
  application:
    name: limits-service
  cloud:
    config:
      uri: ${SPRING_CONFIG_URI:http://localhost:8888}
  profiles:
    active:
      - qa
  devtools:
    livereload:
      port: 35730